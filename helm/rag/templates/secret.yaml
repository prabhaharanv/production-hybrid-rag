apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rag.fullname" . }}-secrets
  labels:
    {{- include "rag.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

apiVersion: v1
kind: Secret
metadata:
  name: rag-api-secrets
  namespace: rag
  labels:
    app.kubernetes.io/name: rag-api
type: Opaque
stringData:
  # Replace these values before applying.
  # In production, use SealedSecrets, SOPS, or an external secret operator.
  OPENAI_API_KEY: "REPLACE_ME"
  OPENAI_BASE_URL: ""
  RAG_API_KEY: ""

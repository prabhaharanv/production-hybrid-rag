apiVersion: v1
kind: Service
metadata:
  name: rag-api
  namespace: rag
  labels:
    app.kubernetes.io/name: rag-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rag-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
